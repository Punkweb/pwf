import { http } from '../http';

describe('http', () => {
  it('should export request', () => {
    expect(http.request).toBeTruthy();
  });

  it('should export buildQueryString', () => {
    expect(http.request).toBeTruthy();
  });
});
